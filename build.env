# Node.js environment
NODE_ENV=production

# Vite build base path
VITE_BASE_URL=/

# Server settings
HOST=0.0.0.0
PORT=5173

# API endpoints (customize if needed)
VITE_API_URL=http://localhost:5000/api

# App info
APP_NAME=Physics Simulation
APP_VERSION=1.0.0

# Localization (if translations.ts is used)
DEFAULT_LANGUAGE=en
